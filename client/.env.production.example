# Copiez ce fichier en ".env.production" dans /client et adaptez les valeurs avant de builder.
# Ces valeurs sont injectées au moment du build (npm run build), pas au runtime.

VITE_API_URL=https://votre-serveur.exemple.com
VITE_WS_URL=wss://votre-serveur.exemple.com
